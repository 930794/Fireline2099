import greenfoot.*;

public class cover extends Actor {
    public cover() {
        setImage(new GreenfootImage(50, 50)); // Simple block for visuals
        getImage().setColor(Color.GRAY);
        getImage().fill();
    }

    public void act() {
        // No interaction unless needed in the future
    }
}
